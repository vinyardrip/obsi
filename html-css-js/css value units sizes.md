
# Единицы измерения в CSS

Таблица для быстрого ориентирования в единицах измерения, их назначении и практическом применении.

| Единица | Расшифровка (для запоминания) | Категория | Основное применение (Ключевой сценарий) | Пример с 10px |
|--------|-------------------------------|-----------|------------------------------------------|----------------|
| `rem` | root em | Основная | Вся типографика, отступы и размеры в адаптивном дизайне. Базовая единица для масштабирования всего интерфейса. | `0.625rem = 10px` |
| `%` | percent | Основная | Жидкостные макеты. Ширина/высота относительно родителя (`width: 50%`). Для шрифтов — относительно `font-size` родителя. | `62.5%` от `16px = 10px` |
| `px` | pixel | Абсолютная | Тонкие границы, тени, мелкая графика. Там, где требуется фиксированная точность, не зависящая от масштабирования. | `10px` — это всегда `10px` |
| `em` | em (от буквы "M") | Относительная | Внутренние отступы и размеры внутри компонентов. Чтобы `padding` масштабировался вместе с `font-size` этого же элемента. | `0.625em` от `16px = 10px` |
| `vw` / `vh` | viewport width / height | Viewport (основные) | Блоки "на весь экран". Создание секций, которые всегда занимают 100% ширины или высоты окна браузера. | `1.04vw ≈ 10px` (ширина окна `960px`) |
| `fr` | fraction (доля) | Для раскладок | CSS Grid. Распределение свободного пространства в пропорциях: `grid-template-columns: 1fr 2fr;` | Не конвертируется в `px`. |
| `ch` | character (символ "0") | Для типографики | Оптимальная длина строки. Установка `max-width: 70ch` для абзацев для лучшей читаемости. | Зависит от шрифта. |
| `lh` | line-height (высота строки) | Для типографики | Вертикальный ритм. Создание отступов, кратных межстрочному интервалу: `margin-bottom: 1lh;` | `1lh = текущий line-height.` |
| `vmin` / `vmax` | viewport minimum / maximum | Viewport (логические) | Квадратные или адаптивные к экрану элементы. Гарантия, что элемент впишется в экран: `height: 100vmin;` | Динамические. |
| `cqw` / `cqi` | container query width / inline-size | Современные (контейнер) | Адаптивность внутри компонента (`Container Queries`). Изменение стилей карточки в зависимости от её собственной ширины. | `1cqi = 10px` (ширина контейнера `1000px`) |
| `vi` / `vb` | viewport inline / block | Современные (логические) | Логические макеты. Создание отступов, зависящих от направления письма (`LTR/RTL`, вертикальные языки). | В `LTR`: `1vi ≈ 1vw.` |
| `svw` / `svh` | smallest viewport width / height | Viewport (мобильные/динамические) | Гарантия видимости. Элемент, который должен точно помещаться в область, видимую при открытой адресной строке/панели навигации. | Зависит от браузера/UI. |
| `lvw` / `lvh` | largest viewport width / height | Viewport (мобильные/динамические) | Заполнение области. Элемент, который должен полностью заполнять область, видимую при скрытой адресной строке/панели навигации. | Зависит от браузера/UI. |
| `dvw` / `dvh` | dynamic viewport width / height | Viewport (мобильные/динамические) | Плавная адаптация (PWA). Фиксированные фоны/контейнеры в мобильных приложениях, которые плавно меняют размер при скролле и скрытии панелей браузера. | Меняется динамически. |
| `cm` / `mm` | centimeter / millimeter | Физические | Стили для печати (`@print`). Задание размеров для физического носителя. На экране ненадежны. | `0.2646cm ≈ 10px` (при `96 DPI`) |
| `pt` / `pc` | point / pica (типографские) | Полиграфические | Исключительно для печати. В веб-отображении использовать не рекомендуется. | `7.5pt ≈ 10px` (при `96 DPI`) |

> **💡 Совет**: Для адаптивных веб-проектов рекомендуется использовать `rem` и `%` как основные единицы, `em` — внутри компонентов, а `vw`/`vh` — для геро-секций и полноэкранных блоков. Современные единицы (`cqi`, `dvh` и др.) активно применяются в PWA и контейнерных адаптивах.