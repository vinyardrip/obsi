
# 🛠️ Шпаргалка: Mise (v2024+)

> [!abstract] Описание
> **Mise** (ранее rtx) — менеджер инструментов на Rust. Быстрее `asdf`, поддерживает синтаксис `имя@версия`.

---

## 1. Основные команды

| Задача                    | Команда                    | Псевдоним |
| :------------------------ | :------------------------- | :-------- |
| **Активные версии**       | `mise ls`                  | `mi ls`   |
| **Что активно сейчас**    | `mise current`             | -         |
| **Список доступных**      | `mise ls-remote <tool>`    | -         |
| **Установить из конфига** | `mise install`             | `mi i`    |
| **Глобальная версия**     | `mise use -g <tool>@<ver>` | `mi g`    |
| **Локальная версия**      | `mise use <tool>@<ver>`    | `mi use`  |
| **Где лежит бинарник**    | `mise which <tool>`        | -         |
| **Путь к папке версии**   | `mise where <tool>`        | -         |
| **Удалить версию**        | `mise uninstall <t>@<v>`   | `mi un`   |
| **Очистить старые** | `mise prune` | - |


## 2. Работа с инструментами (Стабильные версии)

### 🟢 Node.js
**Список последних LTS (v10+):**
```bash
curl -sL [https://nodejs.org/dist/index.tab](https://nodejs.org/dist/index.tab) | awk 'NR>1 && $10 != "-" {split($1, v, "."); if (v[1] >= "v10" && !a[v[1]]++) print $1, "(" $10 ")"}' | tac
````

**Примеры установки:**

- `mise use -g node@lts` (самая свежая LTS)

- `mise use -g node@20` (последняя в 20-й ветке)

- `mise use node@lts/hydrogen` (локально v18)


### 🟣 pnpm

**Список стабильных:**

Bash

```
mise ls-remote pnpm | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' | tail -n 20
```

**Установка:**

- `mise use -g pnpm@latest`
    

### 🐍 Python

**Список мажорных релизов CPython:**

Bash

```
mise ls-remote python | grep -E '^[0-9]+\.[0-9]+' | sort -V | awk -F. '{a[$1"."$2]=$0} END {for (i in a) print a[i]}' | sort -V
```

**Установка:**

- `mise use -g python@latest`
    
- `mise use python@3.12`
    

### 💎 Ruby

**Список стабильных:**

Bash

```
mise ls-remote ruby | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' | awk -F. '{if (!a[$1"."$2]++) print $0}' | tail -n 15
```

**Установка:**

- `mise use -g ruby@latest`
    

### 🦀 Rust

**Список стабильных:**

Bash

```
mise ls-remote rust | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' | sort -V | awk -F. '{a[$1"."$2]=$0} END {for (i in a) print a[i]}' | sort -V | tail -n 15
```


**Установка через каналы:**

- `mise use -g rust@stable`
    
- `mise use rust@1.75.0`
    

---

## 3. Автоматизация (Bash/Zsh)

> [!tip] Алиас для Node.js LTS
> 
> Добавьте в ~/.zshrc или ~/.bashrc:
> 
> Bash
> 
> ```
> alias node-lts="curl -sL [https://nodejs.org/dist/index.tab](https://nodejs.org/dist/index.tab) | awk 'NR>1 && \$10 != \"-\" {split(\$1, v, \".\"); if (v[1] >= \"v10\" && !a[v[1]]++) print \$1, \"(\" \$10 \")\"}' | tac"
> ```

---

## 4. Конфигурационные файлы

|**Файл**|**Описание**|
|---|---|
|`~/.config/mise/config.toml`|Глобальные настройки|
|`.tool-versions`|Формат asdf|
|`mise.toml`|Формат mise|