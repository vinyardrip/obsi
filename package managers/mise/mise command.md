
# 🛠️ Шпаргалка: Mise (v2024+)

> [!abstract] Описание
> **Mise** (ранее rtx) — современный менеджер инструментов на Rust. Быстрее `asdf`, поддерживает синтаксис `имя@версия` и автоматически подхватывает плагины.

---

## 1. Основные команды

| Задача | Команда | Псевдоним |
| :--- | :--- | :--- |
| **Активные версии** | `mise ls` | `mi ls` |
| **Список доступных** | `mise ls-remote <tool>` | - |
| **Установить из конфига** | `mise install` | `mi i` |
| **Глобальная версия** | `mise use -g <tool>@<ver>` | `mi g` |
| **Локальная версия** | `mise use <tool>@<ver>` | `mi use` |
| **Удалить версию** | `mise uninstall <t>@<v>` | `mi un` |
| **Очистить старые** | `mise prune` | - |
| **Путь к бинарнику** | `mise which <tool>` | - |

---

## 2. Работа с инструментами (Только стабильные)

### 🟢 Node.js
Вывод по одной последней версии на мажорную LTS ветку (v10+).

**Показать последние LTS:**
```bash
curl -sL [https://nodejs.org/dist/index.tab](https://nodejs.org/dist/index.tab) | awk 'NR>1 && $10 != "-" {split($1, v, "."); if (v[1] >= "v10" && !a[v[1]]++) print $1, "(" $10 ")"}' | tac
````

**Примеры установки:**

- `mise use -g node@lts` — самая свежая LTS.
    
- `mise use -g node@20` — последний патч 20-й ветки.
    
- `mise use node@lts/hydrogen` — локально установить v18.
    

### 🟣 pnpm

Фильтрация альфа/бета версий.

**Показать только стабильные:**

Bash

```
mise ls-remote pnpm | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' | tail -n 20
```

**Установка:**

- `mise use -g pnpm@latest`
    

### 🐍 Python

Только чистые релизы CPython (без dev, rc и прочих).

**Показать мажорные релизы:**

Bash

```
mise ls-remote python | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' | awk -F. '{if (!a[$1"."$2]++) print $0}' | tail -n 20
```

**Установка:**

- `mise use -g python@latest`
    
- `mise use python@3.12`
    

### 💎 Ruby

**Показать список стабильных:**

Bash

```
mise ls-remote ruby | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' | awk -F. '{if (!a[$1"."$2]++) print $0}' | tail -n 15
```

### 🦀 Rust

**Установка через каналы:**

- `mise use -g rust@stable`
    
- `mise use rust@1.75.0`
    

---

## 3. Автоматизация (Bash/Zsh)

> [!tip] Полезный Алиас
> 
> Добавьте в ~/.zshrc или ~/.bashrc для быстрой проверки Node.js LTS командой node-lts:
> 
> Bash
> 
> ```
> alias node-lts="curl -sL [https://nodejs.org/dist/index.tab](https://nodejs.org/dist/index.tab) | awk 'NR>1 && \$10 != \"-\" {split(\$1, v, \".\"); if (v[1] >= \"v10\" && !a[v[1]]++) print \$1, \"(\" \$10 \")\"}' | tac"
> ```

---

## 4. Конфигурационные файлы

| **Файл**                     | **Описание**                  |
| ---------------------------- | ----------------------------- |
| `~/.config/mise/config.toml` | Глобальные настройки и версии |
| `.tool-versions`             | Стандартный файл `asdf`       |
| `mise.toml`                  | Современный формат конфига    |