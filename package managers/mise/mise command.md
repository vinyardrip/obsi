
# 🛠️ Шпаргалка: Mise (v2024+)

> [!abstract] Описание
> **Mise** (ранее rtx) — это современный менеджер инструментов, написанный на Rust. Он полностью заменяет `asdf`, работает быстрее и поддерживает более удобный синтаксис `имя@версия`.

---

## 1. Основные команды управления

| Задача | Команда | Псевдоним | Описание |
| :--- | :--- | :--- | :--- |
| **Активные версии** | `mise ls` | `mi ls` | Список того, что используется сейчас |
| **Список доступных** | `mise ls-remote <tool>` | - | Показать всё, что можно скачать |
| **Установить из конфига**| `mise install` | `mi i` | Ставит всё из `.tool-versions` или `mise.toml` |
| **Локальная версия** | `mise use <tool>@<ver>` | `mi use` | Создает/обновляет файл в текущей папке |
| **Глобальная версия** | `mise use -g <tool>@<ver>`| `mi g` | Устанавливает версию для всей системы |
| **Удалить версию** | `mise uninstall <t>@<v>` | `mi un` | Удаляет скачанные файлы версии |
| **Очистка старых** | `mise prune` | - | Удаляет все версии, которые не используются |
| **Путь к бинарнику** | `mise which <tool>` | - | Показывает полный путь к исполнителю |

---

## 2. Работа с инструментами (Только стабильные/LTS)

### 🟢 Node.js
Используем фильтрацию официального индекса, чтобы видеть по одной последней версии на каждую мажорную ветку (от 10 и выше).

* **Показать последние LTS:**
  ```bash
  curl -sL [https://nodejs.org/dist/index.tab](https://nodejs.org/dist/index.tab) | awk 'NR>1 && $10 != "-" {split($1, v, "."); if (v[1] >= "v10" && !a[v[1]]++) print $1, "(" $10 ")"}' | tac
```

- **Примеры установки:**
    
    - `mise use -g node@lts` — самая новая LTS
        
    - `mise use -g node@20` — последний патч 20-й ветки
        
    - `mise use node@lts/hydrogen` — локально установить v18
        

### 🟣 pnpm

Фильтруем альфа/бета версии, оставляя только чистые релизы.

- **Показать последние стабильные:**
    
    Bash
    
    ```
    mise ls-remote pnpm | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' | tail -n 20
    ```
    
- **Установка:**
    
    - `mise use -g pnpm@latest`
        

### 🐍 Python

Скрываем сотни специфичных сборок, оставляя только основные версии CPython.

- **Показать мажорные релизы:**
    
    Bash
    
    ```
    mise ls-remote python | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' | awk -F. '{if (!a[$1"."$2]++) print $0}' | tail -n 20
    ```
    
- **Установка:**
    
    - `mise use -g python@latest`
        
    - `mise use python@3.12`
        

### 💎 Ruby

Аналогичная фильтрация для чистых версий Ruby.

- **Показать список стабильных:**
    
    Bash
    
    ```
    mise ls-remote ruby | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' | awk -F. '{if (!a[$1"."$2]++) print $0}' | tail -n 15
    ```
    

### 🦀 Rust

Rust лучше всего управлять через официальные каналы обновления.

- **Установка:**
    
    - `mise use -g rust@stable` — всегда стабильная
        
    - `mise use rust@1.75.0` — конкретная версия
        

---

## 3. Автоматизация (Bash/Zsh)

> [!tip] Полезный Алиас
> 
> Добавьте это в ваш ~/.zshrc или ~/.bashrc, чтобы быстро смотреть LTS версии Node.js командой node-lts:
> 
> Bash
> 
> ```
> alias node-lts="curl -sL [https://nodejs.org/dist/index.tab](https://nodejs.org/dist/index.tab) | awk 'NR>1 && \$10 != \"-\" {split(\$1, v, \".\"); if (v[1] >= \"v10\" && !a[v[1]]++) print \$1, \"(\" \$10 \")\"}' | tac"
> ```

---

## 4. Конфигурационные файлы

|**Файл**|**Описание**|
|---|---|
|`~/.config/mise/config.toml`|Глобальные настройки и версии|
|`.tool-versions`|Стандартный файл `asdf` (поддерживается `mise`)|
|`mise.toml`|Новый формат конфига (более мощный)|