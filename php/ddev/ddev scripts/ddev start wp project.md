
```bash
#!/usr/bin/env bash
# =================================================================
# Скрипт для создания нового WordPress проекта на DDEV "с нуля".
# ВЕРСИЯ 2.1: Добавлена автоматическая остановка Devilbox.
#
# Что делает:
# 1. Останавливает Devilbox для предотвращения конфликта портов.
# 2. Спрашивает имя проекта.
# 3. Устанавливает и настраивает WordPress.
# 4. Копирует, генерирует style.css и активирует стартовую тему.
# 5. Открывает сайт в браузере.
# =================================================================

set -e

# --- ⚙️ НАСТРОЙТЕ ЭТИ ПЕРЕМЕННЫЕ ПОД СЕБЯ ---
DDEV_PROJECTS_BASE_PATH="$HOME/projects/work/php"
STARTER_THEME_PATH="$HOME/projects/work/_s-template"
ADMIN_USER="admin"
ADMIN_EMAIL="admin@example.com"
ADMIN_PASS="admin"
# Добавляем путь к Devilbox
DEVILBOX_PATH_ROOT="$HOME/projects/work/devilbox"
# --- Конец настроек ---

# --- Утилиты и цвета ---
if command -v tput >/dev/null 2>&1; then
    GREEN=$(tput setaf 2); YELLOW=$(tput setaf 3); BLUE=$(tput setaf 4); RED=$(tput setaf 1); NC=$(tput sgr0)
else
    GREEN='\033[0;32m'; YELLOW='\033[1;33m'; BLUE='\033[0;34m'; RED='\033[0;31m'; NC='\033[0m'
fi

# --- Логика скрипта ---
printf "%s--- Создание нового проекта WordPress ---%s\n" "${BLUE}" "${NC}"

# === НОВЫЙ БЛОК: Предотвращение конфликта портов ===
if [ -d "$DEVILBOX_PATH_ROOT" ]; then
    printf "\n%s--- Проверка состояния Devilbox ---%s\n" "${BLUE}" "${NC}"
    printf "Чтобы избежать конфликта портов, Devilbox будет остановлен...\n"
    (cd "$DEVILBOX_PATH_ROOT" && docker-compose down >/dev/null 2>&1)
    printf "✔ Devilbox успешно остановлен.\n"
fi
# === КОНЕЦ НОВОГО БЛОКА ===

read -p "1. Введите имя нового проекта (например, my-cool-site): " PROJECT_NAME
if [ -z "$PROJECT_NAME" ]; then
    printf "%sОШИБКА: Имя проекта не может быть пустым.%s\n" "${RED}" "${NC}"
    exit 1
fi

PROJECT_PATH="${DDEV_PROJECTS_BASE_PATH}/${PROJECT_NAME}"
SITE_URL="https://${PROJECT_NAME}.lo"

if [ -d "$PROJECT_PATH" ]; then
    printf "%sВНИМАНИЕ: Директория '%s' уже существует. Операция отменена.%s\n" "${YELLOW}" "${PROJECT_PATH}" "${NC}"
    exit 1
fi

printf "\n%sНачинаем создание проекта '%s'...%s\n" "${GREEN}" "${PROJECT_NAME}" "${NC}"

printf "Шаг 1: Создание директории проекта...\n"
mkdir -p "$PROJECT_PATH"
cd "$PROJECT_PATH"

printf "Шаг 2: Конфигурация и запуск DDEV...\n"
ddev config --project-type wordpress --docroot "" --project-tld "lo"
ddev start

printf "Шаг 3: Загрузка и установка WordPress...\n"
ddev wp core download --locale=ru_RU
printf "✔ Гарантированная очистка базы данных перед установкой...\n"
ddev wp db reset --yes
ddev wp core install --url="$SITE_URL" --title="Новый сайт ${PROJECT_NAME}" --admin_user="$ADMIN_USER" --admin_password="$ADMIN_PASS" --admin_email="$ADMIN_EMAIL"

if [ -d "$STARTER_THEME_PATH" ]; then
    printf "Шаг 4: Копирование и активация стартовой темы '%s'...\n" "$PROJECT_NAME"
    THEME_DIR="wp-content/themes/${PROJECT_NAME}"
    cp -r "${STARTER_THEME_PATH}/." "$THEME_DIR"
    if [ ! -f "${THEME_DIR}/style.css" ]; then
        printf "✔ Файл style.css не найден. Создаем его автоматически...\n"
        cat << EOF > "${THEME_DIR}/style.css"
/*
Theme Name: ${PROJECT_NAME}
Author: Auto-generated by DDEV Script
Version: 1.0
*/
EOF
    fi
    ddev wp theme activate "$PROJECT_NAME"
    printf "✔ Стандартные темы удаляются для чистоты проекта...\n"
    ddev wp theme delete twentytwentytwo twentytwentythree twentytwentyfour >/dev/null 2>&1 || true
else
    printf "%sВНИМАНИЕ: Путь к стартовой теме не указан. Шаг пропущен.%s\n" "${YELLOW}" "${NC}"
fi

printf "Шаг 5: Очистка стандартных плагинов...\n"
ddev wp plugin delete akismet hello >/dev/null 2>&1 || true

printf "\n%s🎉 Проект '%s' успешно создан! 🎉%s\n" "${GREEN}" "${PROJECT_NAME}" "${NC}"
echo "------------------------------------------------"
printf "Сайт доступен: %s%s%s\n" "${YELLOW}" "${SITE_URL}" "${NC}"
printf "Админ-панель:  %s%s/wp-admin/%s\n" "${YELLOW}" "${SITE_URL}" "${NC}"
printf "Логин:         %s%s%s\n" "${YELLOW}" "${ADMIN_USER}" "${NC}"
printf "Пароль:        %s%s%s\n" "${YELLOW}" "${ADMIN_PASS}" "${NC}"
printf "Путь к файлам: %s%s%s\n" "${YELLOW}" "${PROJECT_PATH}" "${NC}"
echo "------------------------------------------------"

ddev launch

exit 0