
# 🐘 DDEV Snapshots (Бэкапы)

**Snapshot** — это встроенный механизм DDEV для мгновенного сохранения состояния базы данных. Это "Quick Save" для разработки. 
Идеально подходит для экспериментов, когда нужно быстро откатиться назад, если что-то пошло не так.

> [!INFO] Где хранятся файлы?
> Снапшоты лежат внутри проекта в скрытой папке:
> `/.ddev/db_snapshots/`

---

## ⚡️ Основные команды

### 1. Создать снимок (Бэкап)
Сохраняет текущее состояние БД.

```bash
# Быстрый снимок (имя сгенерируется автоматически по дате)
ddev snapshot

# Снимок с понятным именем (Рекомендуется)
ddev snapshot --name "before_update"
ddev snapshot -n "test_import"
2. Восстановить снимок (Откат)
⚠️ Внимание: Текущая база данных будет полностью удалена и заменена версией из снимка.
code
Bash
￼
download
￼
content_copy
￼
expand_less
# Показать список и выбрать (если имя не указано)
ddev restore-snapshot

# Восстановить конкретный снимок сразу
ddev restore-snapshot before_update
3. Управление снимками
code
Bash
￼
download
￼
content_copy
￼
expand_less
# Показать список всех снимков проекта
ddev snapshot -a

# Удалить конкретный снимок
ddev delete-snapshot before_update

# Удалить ВСЕ снимки проекта (очистка места)
ddev delete-snapshot --all
🛠 Типичный сценарий (Workflow)
Перед опасным действием (обновление WordPress, прямой SQL-запрос):
code
Bash
￼
download
￼
content_copy
￼
expand_less
ddev snapshot -n "pre_experiment"
...Делаем изменения, всё ломается... 💥
Откатываемся назад:
code
Bash
￼
download
￼
content_copy
￼
expand_less
ddev restore-snapshot pre_experiment
Если всё прошло успешно, удаляем старый бэкап (опционально):
code
Bash
￼
download
￼
content_copy
￼
expand_less
ddev delete-snapshot pre_experiment
code
Code
￼
download
￼
content_copy
￼
expand_less
### Пара советов по использованию:

1.  **Называйте снимки понятно.** Имя `snapshot_20231025` вам ничего не скажет через неделю. Имя `before_woo_update` скажет всё.
2.  **Это не для продакшена.** Эти снапшоты хранятся в несжатом или слабо сжатом виде внутри папки проекта. Не используйте это как долгосрочное хранилище бэкапов (для этого лучше `.sql.gz` файлы из предыдущего скрипта), так как папка `.ddev` может сильно разрастись.
3.  **Git.** Обычно папка `.ddev/db_snapshots` добавлена в `.gitignore` по умолчанию самим DDEV, поэтому эти огромные дампы не улетят в репозиторий. Но проверить стоит.