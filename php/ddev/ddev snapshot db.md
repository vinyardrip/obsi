

# 🐘 DDEV Snapshots (Бэкапы)

**Snapshot** — это встроенный механизм DDEV для мгновенного сохранения состояния базы данных. Это "Quick Save" для разработки. 
Идеально подходит для экспериментов, когда нужно быстро откатиться назад, если что-то пошло не так.

> [!INFO] Где хранятся файлы?
> Снапшоты лежат внутри папки проекта (скрытая директория):
> `/.ddev/db_snapshots/`
> 
> *Физически это `.gz` архивы с SQL-дампом.*

---

## ⚡️ Основные команды

### 1. Создать снимок (Бэкап)
Сохраняет текущее состояние БД.

```bash
# Быстрый снимок (имя сгенерируется автоматически по дате)
ddev snapshot

# Снимок с понятным именем (Рекомендуется!)
ddev snapshot --name "before_update"
ddev snapshot -n "test_import"
````

### 2. Управление снимками

codeBash

```
# Показать список всех снимков проекта
ddev snapshot -a

# Удалить конкретный снимок
ddev delete-snapshot before_update

# Удалить ВСЕ снимки проекта (очистка места)
ddev delete-snapshot --all
```

---

## 🆘 Экстренное восстановление (Если всё сломалось)

Если эксперимент не удался, сайт упал, или вы случайно удалили нужные таблицы — действуем по этому алгоритму.

> [!WARNING] Внимание!  
> Команда восстановления **полностью удаляет** текущую базу данных и заменяет её версией из снимка. Все данные, созданные после снимка, будут потеряны.

### Сценарий А: Стандартный откат

**1. Проверяем, какие есть бэкапы:**

codeBash

```
ddev snapshot -a
```

Вывод покажет список имен, например: before_update, snapshot_20231025.

**2. Восстанавливаем нужный снимок:**

codeBash

```
ddev restore-snapshot before_update
```

**3. Перезапускаем сайт (для надежности):**  
Иногда после подмены базы полезно сбросить кеши или перезагрузить контейнеры.

codeBash

```
ddev restart
```

---

### Сценарий Б: "Я не помню имя снимка"

Если вы не указывали имя (-n), DDEV создает их с именами типа snapshot_YYYYMMDD_HHMMSS. Чтобы не вводить это вручную:

1. Введите команду без аргументов:
    
    
 ```sh
   ddev restore-snapshot
   ```
    
2. DDEV покажет интерактивный список.
    
3. Выберите нужный номер и нажмите Enter.
    

---

### Сценарий В: "Ручное" восстановление (Если DDEV глючит)

Если команда restore-snapshot почему-то не работает, но файлы снимков существуют, можно восстановить базу вручную.

**1. Найдите файл снимка:**  
Зайдите в папку проекта: /.ddev/db_snapshots/.  
Там лежат файлы вида before_update.sql.gz или папки (для MariaDB 10.5+).

**2. Импортируйте его как обычный дамп:**  
Если это файл .gz:

codeBash

```
ddev import-db --file=.ddev/db_snapshots/before_update.sql.gz
```

---

## 🛠 Типичный Workflow (Безопасная работа)

1. **Перед опасным действием** (обновление плагинов, прямой SQL-запрос):
    
    codeBash
    
    ```
    ddev snapshot -n "pre_experiment"
    ```
    
2. ...Делаем изменения, всё ломается, сайт лежит... 💥
    
3. **Откатываемся назад**:
    
    codeBash
    
    ```
    ddev restore-snapshot pre_experiment
    ```
    
4. **Если всё прошло успешно**, удаляем старый бэкап (чтобы не занимал место):
    
    codeBash
    
    ```
    ddev delete-snapshot pre_experiment
    ```
    

---

## 🛡 Git и Snapshots

Обычно папка .ddev/db_snapshots добавлена в .gitignore глобально самим DDEV.  
Это значит, что **снимки не попадают в репозиторий Git**.

- **Плюс:** Репозиторий не раздувается от тяжелых дампов.
    
- **Минус:** Если вы удалите папку проекта с диска, снимки удалятся вместе с ней.
    

> [!TIP] Совет  
> Если нужен "долговечный" бэкап для передачи коллеге, лучше используйте ddev export-db и сохраните файл в явную папку (например, /db_dumps).